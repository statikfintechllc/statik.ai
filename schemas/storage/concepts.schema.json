{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "statik://schemas/storage/concepts",
  "title": "Concept",
  "description": "Semantic knowledge – facts and definitions",
  "type": "object",
  "required": ["id", "name", "definition", "confidence"],
  "properties": {
    "id":         { "type": "string" },
    "name":       { "type": "string" },
    "definition": { "type": "string" },
    "relations":  { "type": "array", "items": { "type": "string" } },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
  }
}
